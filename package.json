{
  "name": "isbnnet-resolver",
  "version": "1.0.0",
  "description": "Cloudflare Worker microservice for Taiwan NCL ISBN-net lookup",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.0",
    "@types/node": "^26.3.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "^4.111.0"
  }
}
